---
- name: "Configure jails"
  include_tasks:
    file: "configure_jail.yml"
    apply:
      tags:
        - "jails"
  vars:
    jail: "{{ item.value }}"
    jailname: "{{ item.value.hostname|replace('.','_') }}"
  loop: "{{ jails }}"
  tags:
    - "jails"
