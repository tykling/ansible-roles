---
- name: "Make sure syslog-ng newsyslog config file exists"
  copy:
    owner: "root"
    group: "wheel"
    mode: "755"
    src: "syslog-ng-newsyslog.conf"
    dest: "/usr/local/etc/newsyslog.conf.d/syslog-ng.conf"

