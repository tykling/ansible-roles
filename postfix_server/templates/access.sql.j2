hosts={{ postgres_host }}:{{ postgres_port }}
user={{ postgres_user }}
password={{ postgres_password }}
dbname={{ postgres_dbname }}
query = SELECT action from access where lower(client) = lower('%s') and enabled = 't'
