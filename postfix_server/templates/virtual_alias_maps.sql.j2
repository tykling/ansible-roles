hosts={{ postgres_host }}:{{ postgres_port }}
user={{ postgres_user }}
password={{ postgres_password }}
dbname={{ postgres_dbname }}
query = SELECT destination from virtual_alias_maps where lower(localpart||'@'||domain) = lower('%s') and enabled = 't'

