---
- name: "Make sure /usr/local/etc/newsyslog.conf.d/ exists"
  file:
    path: "/usr/local/etc/newsyslog.conf.d"
    state: "directory"

- name: "Make sure openbpgd newsyslog config file exists"
  copy:
    owner: "root"
    group: "wheel"
    mode: "755"
    src: "openbgpd-newsyslog.conf"
    dest: "/usr/local/etc/newsyslog.conf.d/openbgpd"

