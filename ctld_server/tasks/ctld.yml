---
- name: "Enable ctld"
  service:
    name: "ctld"
    enabled: yes
  notify: "start ctld"

- name: "Copy ctld config"
  template:
    src: "ctl.conf.j2"
    dest: "/etc/ctl.conf"
  notify: "reload ctld"

