---
dependencies:
  - { role: handlers }
  - { role: certgrinder_client, when: (nginx_proxy and certgrinder_hostname_sets is defined) }
  - { role: dhparams_server, when: (nginx_proxy and certgrinder_hostname_sets is defined) }

