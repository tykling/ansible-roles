---
- name: "Make sure /usr/local/etc/newsyslog.conf.d/ exists"
  file:
    path: "/usr/local/etc/newsyslog.conf.d"
    state: "directory"

- name: "Make sure circusd newsyslog config file exists"
  copy:
    owner: "root"
    group: "wheel"
    mode: "755"
    src: "circusd-newsyslog.conf"
    dest: "/usr/local/etc/newsyslog.conf.d/circusd"

